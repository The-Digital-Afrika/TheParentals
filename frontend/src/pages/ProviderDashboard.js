// frontend/src/pages/ProviderDashboard.js
// Provider dashboard uses the existing dashboard implementation.
// ClientDashboard already renders the provider experience for provider accounts.
import ClientDashboard from './ClientDashboard';

export default ClientDashboard;
